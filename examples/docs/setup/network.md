# Network Configuration

Configure the device network so it can reach the licensing server and sync
results.

## Static IP

Assign a static IP under Settings → Network. Enter the address, subnet mask, and
gateway provided by your administrator.

## Firewall ports

Open outbound TCP 443 to the licensing server. No inbound ports are required for
normal operation.
